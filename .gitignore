__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.DS_Store
# config contains the API key — never commit it
~/.dictaflow/
# downloaded model weights — far too large for git (1.6GB+)
models/
# runtime logs from the LaunchAgent
*.log

# transcripts live in ~/transcriptions, never in the repo
transcriptions/

# built app bundle — rebuild with app-src/build.sh
DictaFlow.app/
